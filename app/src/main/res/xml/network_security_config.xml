<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext (HTTP) liberado apenas para o backend local de desenvolvimento:
         10.0.2.2 é o endereço do host visto de dentro do emulador Android;
         localhost/127.0.0.1 cobre execução em dispositivo físico com adb reverse.
         Qualquer outro host segue a configuração padrão (HTTPS obrigatório). -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
